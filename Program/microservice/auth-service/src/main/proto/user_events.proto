syntax = "proto3";

package com.pm.events;

option java_multiple_files = true;
option java_package = "com.pm.events.user";

message UserRegisteredEvent {
  string userId = 1;
  string email = 2;
  // Add other initial fields you want to share, e.g., name.
  // Avoid sending sensitive data like the password.
}